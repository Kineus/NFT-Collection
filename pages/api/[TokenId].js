// Next.js API route support: https://nextjs.org/docs/api-routes/introduction

export default function handler(req, res) {
 const tokenId = req.query.tokenId;
 
 const name = 'Noisy Investor #${tokenId}';
 const description = "Noisy Investor collection is an NFT Collection for all Students in the Noisy Investor Academy";
 const image = "";
}
